Real App Store icon artwork, used by the work section in `index.html`.

Square, unmasked, 192x192 PNG. The corner radius is applied by CSS in
`index.html` (`img.mark`), so do not bake rounded corners into the file.

Only shipped apps belong here. Unshipped ones use the inline SVG marks.
